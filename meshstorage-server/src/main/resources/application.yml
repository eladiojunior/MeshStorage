# Configuracao do servidor
# -----------------------------------------------------
server:
  port: ${SERVER_PORT}

#spring:
  # Variaveis de conexão com os bancos de dados
  # -----------------------------------------------------
  # MongoDB - Armazenar histórico de acessos
#  data:
#    mongodb:
#      uri: mongodb://${DB_MONGO_SERVER}:${DB_MONGO_PORT}/${DB_MONGO_DBNAME}
#      username: ${DB_MONGO_USERNAME}
#      password: ${DB_MONGO_PASSWORD}

# Configuração do swagger para apresentar a documentação da API
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html